(() => {const csrf=document.querySelector('meta[name="csrf-token"]')?.content||'',toast=document.getElementById('favorites-toast');function show(t){toast.textContent=t;toast.classList.add('show');clearTimeout(show.x);show.x=setTimeout(()=>toast.classList.remove('show'),1700)}document.querySelectorAll('.remove-favorite').forEach(btn=>btn.addEventListener('click',async()=>{const card=btn.closest('.favorite-card'),platform=card.dataset.platform;btn.disabled=true;try{const r=await fetch('/api/favorites/toggle',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},body:JSON.stringify({platform})}),d=await r.json();if(!r.ok)throw new Error(d.error||'操作失败');card.remove();const count=document.getElementById('favorite-count');count.textContent=Math.max(0,Number(count.textContent)-1);show('已取消收藏');if(!document.querySelector('.favorite-card'))location.reload()}catch(e){show(e.message);btn.disabled=false}}))})();
